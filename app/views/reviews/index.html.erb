<% if !@reviews_customer.nil? or !@reviews_restaurant.nil? %>

  <h2>Listing reviews</h2>
  <br/><br/>
  <% if customersigned_in? %>
    <table class="table">
      <tr>
        <th width="35%">Restaurant</th>
        <th width="40%">Your feedback</th>
        <th width="25%"></th>
      </tr>

    <% @reviews_customer.each do |review| %>
      <% @this_restaurant = Restaurant.find_by_id(review.restaurant_id) %>
      <tr>
        <td width="25%"><%= link_to @this_restaurant.name, @this_restaurant, class: "mylinks" %></td>
        <td width="50%"><%= review.user_feedback %></td>
        <td width="25%"><%= link_to 'Delete', review, method: :delete, data: { confirm: 'Are you sure?' },class: "btn-primary" %></td>
      </tr>
    <% end %>
    </table>

  <% elsif restaurantsigned_in? %>
    <table class="table">
    <tr>
      <th width="40%">User</th>
      <th width="60%">User feedback</th>
    </tr>

    <% @reviews_restaurant.each do |review| %>
      <% @this_customer = Customer.find_by_id(review.user_id) %>
      <tr>
        <td width="40%"><%= link_to @this_customer.name, @this_customer , class: "mylinks_reviews"%></td>
        <td width="60%"><%= review.user_feedback %></td>
      </tr>
    <% end %>
    </table>
  <%end%>

<%else%>
 <h2> Sorry !! You don't have any reviews </h2>

 <%end%>



<br />

